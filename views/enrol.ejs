<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zero→Hero: Software Engineering</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>
</head>
<body class="bg-gradient-to-br from-zinc-50 via-white to-indigo-50 text-zinc-800">
  <!-- Navbar -->
  <nav class="sticky top-0 z-50 backdrop-blur bg-white/60 border-b">
    <div class="max-w-6xl mx-auto flex items-center justify-between py-3 px-6">
      <div class="flex items-center gap-2">
        <div class="h-9 w-9 grid place-items-center rounded-xl bg-gradient-to-br from-indigo-600 to-purple-600 text-white shadow">🎓</div>
        <span class="font-bold">Zero→Hero: Software Engineering</span>
        <span class="ml-2 px-2 py-1 text-xs bg-indigo-100 text-indigo-700 rounded">Beginner Friendly</span>
      </div>
      <div class="hidden sm:flex items-center gap-3">
        <a href="#curriculum" class="text-sm hover:underline">Curriculum</a>
        <a href="#projects" class="text-sm hover:underline">Projects</a>
        <a href="#pricing" class="text-sm hover:underline">Pricing</a>
        <a href="#faq" class="text-sm hover:underline">FAQ</a>
        <a href="#pricing"><button class="ml-2 bg-indigo-600 text-white px-4 py-2 rounded-xl">Enroll Now</button></a>
      </div>
    </div>
  </nav>

  <!-- Hero -->
  <section class="max-w-6xl mx-auto py-12 px-6 grid lg:grid-cols-2 gap-10 items-center">
    <div>
      <h1 class="text-3xl sm:text-5xl font-extrabold leading-tight">Teach <span class="bg-clip-text text-transparent bg-gradient-to-r from-indigo-600 to-violet-600">Software Engineering</span> to Absolute Beginners — Visually & Interactively</h1>
      <p class="mt-4 text-zinc-600">A single-page, high-converting landing page to explain Web Dev, Java & DSA with visuals, tabs, and a clear purchase flow.</p>
      <div class="mt-6 flex flex-wrap items-center gap-3">
      </div>
      <div class="mt-8 grid grid-cols-2 sm:grid-cols-3 gap-4 text-center">
        <div><div class="text-3xl font-bold text-indigo-600">10+</div><p class="text-sm">Interactive Lessons</p></div>
        <div><div class="text-3xl font-bold text-indigo-600">5</div><p class="text-sm">Capstone Projects</p></div>
        <div><div class="text-3xl font-bold text-indigo-600">100%</div><p class="text-sm">Beginner Friendly</p></div>
      </div>
    </div>

    <div class="relative bg-gradient-to-br from-indigo-600/20 to-purple-600/20 p-6 rounded-3xl shadow-xl">
      <div class="grid grid-cols-2 gap-4">
        <div class="bg-white rounded-xl p-4 shadow">
          <h3 class="font-semibold flex items-center gap-2">💻 Frontend</h3>
          <p class="text-sm text-zinc-600">HTML • CSS • JS • React • Tailwind</p>
        </div>
        <div class="bg-white rounded-xl p-4 shadow">
          <h3 class="font-semibold flex items-center gap-2">⚙ Backend</h3>
          <p class="text-sm text-zinc-600">Node • Express • DB • APIs</p>
        </div>
        <div class="col-span-2 bg-white rounded-xl p-4 shadow">
          <h3 class="font-semibold flex items-center gap-2">🧠 DSA & Java</h3>
          <p class="text-sm text-zinc-600">Arrays • Strings • Trees • Graphs • OOPs • Spring Boot Basics</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Curriculum -->
  <section id="curriculum" class="max-w-6xl mx-auto py-12 px-6">
    <h2 class="text-2xl font-bold mb-6">Interactive Curriculum</h2>
    <div class="grid md:grid-cols-3 gap-6">
      <div class="bg-white rounded-xl p-6 shadow">
        <h3 class="font-semibold">🌐 Web Development</h3>
        <p class="text-sm text-zinc-600">HTML, CSS, JS, React, Node, Express, MongoDB</p>
      </div>
      <div class="bg-white rounded-xl p-6 shadow">
        <h3 class="font-semibold">☕ Java Backend</h3>
        <p class="text-sm text-zinc-600">OOPs, JDBC, Spring Boot, Hibernate</p>
      </div>
      <div class="bg-white rounded-xl p-6 shadow">
        <h3 class="font-semibold">🧩 DSA & Interview</h3>
        <p class="text-sm text-zinc-600">Arrays, Strings, Recursion, Trees, Graphs, DP</p>
      </div>
    </div>
  </section>

  <!-- Projects -->
  <section id="projects" class="max-w-6xl mx-auto py-12 px-6">
    <h2 class="text-2xl font-bold mb-6">Projects</h2>
    <div class="grid md:grid-cols-2 gap-6">
      <div class="bg-white p-6 rounded-xl shadow">
        <h3 class="font-semibold">Campus Connected (MERN)</h3>
        <p class="text-sm text-zinc-600">Realtime chat, events, JWT auth, MongoDB Atlas</p>
      </div>
      <div class="bg-white p-6 rounded-xl shadow">
        <h3 class="font-semibold">Uber Clone API</h3>
        <p class="text-sm text-zinc-600">Ride booking, Maps, role-based access</p>
      </div>
      <div class="bg-white p-6 rounded-xl shadow">
        <h3 class="font-semibold">Groww SIP Calculator</h3>
        <p class="text-sm text-zinc-600">React + charts, investment projections</p>
      </div>
      <div class="bg-white p-6 rounded-xl shadow">
        <h3 class="font-semibold">Banking System (Java)</h3>
        <p class="text-sm text-zinc-600">Spring Boot REST APIs, MySQL, auth</p>
      </div>
    </div>
  </section>

  <!-- Pricing -->
  <section id="pricing" class="max-w-6xl mx-auto py-12 px-6">
    <h2 class="text-2xl font-bold mb-6">Simple Pricing</h2>
    <div class="grid md:grid-cols-3 gap-6">
      <div class="bg-white rounded-xl p-6 shadow">
        <h3 class="font-semibold">Starter</h3>
        <p class="text-3xl font-bold text-indigo-600">₹999 / month</p>
        <ul class="mt-2 text-sm text-zinc-600 space-y-1">
          <li>✔ HTML, CSS, JS Basics</li>
          <li>✔ 1 Project</li>
          <li>✔ Community Doubts</li>
        </ul>
        <button onclick="openEnrollForm('Starter', 999)" class="mt-4 w-full bg-indigo-600 text-white px-4 py-2 rounded-xl">Enroll</button>
      </div>
      <div class="bg-white rounded-xl p-6 shadow border-2 border-indigo-600">
        <h3 class="font-semibold">Pro (Most Popular)</h3>
        <p class="text-3xl font-bold text-indigo-600">₹2,599 / month</p>
        <ul class="mt-2 text-sm text-zinc-600 space-y-1">
          <li>✔ MERN + Java Fundamentals</li>
          <li>✔ 3 Projects + Resume</li>
          <li>✔ Weekly Live Doubts</li>
        </ul>
        <button onclick="openEnrollForm('Pro', 2599)" class="mt-4 w-full bg-indigo-600 text-white px-4 py-2 rounded-xl">Enroll</button>
      </div>
      <div class="bg-white rounded-xl p-6 shadow">
        <h3 class="font-semibold">Placement</h3>
        <p class="text-3xl font-bold text-indigo-600">₹5,999 / month</p>
        <ul class="mt-2 text-sm text-zinc-600 space-y-1">
          <li>✔ Full MERN + Spring Boot + full java backend + core subjects</li>
          <li>✔ 5 Projects + Mock Interviews</li>
          <li>✔ Resume Review</li>
        </ul>
        <button onclick="openEnrollForm('Placement', 5999)" class="mt-4 w-full bg-indigo-600 text-white px-4 py-2 rounded-xl">Enroll</button>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section id="faq" class="max-w-6xl mx-auto py-12 px-6">
    <h2 class="text-2xl font-bold mb-6">FAQ</h2>
    <div class="space-y-4">
      <details class="bg-white p-4 rounded-xl shadow">
        <summary class="font-semibold cursor-pointer">Is this suitable for beginners?</summary>
        <p class="text-sm text-zinc-600 mt-2">Yes! No prior coding knowledge required. We start from zero and build step by step.</p>
      </details>
      <details class="bg-white p-4 rounded-xl shadow">
        <summary class="font-semibold cursor-pointer">Which stack will I learn?</summary>
        <p class="text-sm text-zinc-600 mt-2">MERN for full‑stack and Java/Spring Boot for backend + DSA prep.</p>
      </details>
      <details class="bg-white p-4 rounded-xl shadow">
        <summary class="font-semibold cursor-pointer">Will I get projects and resume help?</summary>
        <p class="text-sm text-zinc-600 mt-2">Yes—capstone projects, GitHub portfolio tips, and resume-ready sections included.</p>
      </details>
    </div>
  </section>

  <!-- Footer -->
  <footer class="border-t mt-12">
    <div class="max-w-6xl mx-auto py-6 px-6 flex flex-col sm:flex-row items-center justify-between text-sm text-zinc-600">
      <div class="flex gap-4">
        <a href="#">GitHub</a>
        <a href="#">LinkedIn</a>
      </div>
      <div>© 2025 Zero→Hero. All rights reserved.</div>
    </div>
  </footer>
  <!-- Enrollment Modal -->
  <div id="enrollModal" class="hidden fixed inset-0 bg-black/50 flex items-center justify-center z-50 px-4">
    <div class="bg-white p-6 rounded-xl w-full max-w-md relative shadow-lg">
      <h2 class="text-xl font-bold mb-2">Enroll Now</h2>
      <form id="enrollForm" class="space-y-3">
        <input type="hidden" id="courseName">
        <input type="hidden" id="courseAmount">

        <div>
          <label class="text-sm">Name</label>
          <input id="studentName" class="w-full border p-2 rounded" required />
        </div>
        <div>
          <label class="text-sm">Email</label>
          <input id="studentEmail" type="email" class="w-full border p-2 rounded" required />
        </div>
        <div>
          <label class="text-sm">Phone</label>
          <input id="studentPhone" class="w-full border p-2 rounded" required />
        </div>

        <p class="text-sm">Course: <span id="showCourse"></span> | Amount: ₹<span id="showAmount"></span></p>

        <div class="flex gap-2">
          <button type="submit" class="flex-1 bg-indigo-600 text-white py-2 rounded">Proceed to Pay</button>
          <button type="button" onclick="closeEnrollForm()" class="flex-1 bg-gray-200 text-gray-700 py-2 rounded">Cancel</button>
        </div>
      </form>
      <button onclick="closeEnrollForm()" class="absolute top-3 right-3 text-gray-500">✖</button>
    </div>
  </div>
  <!-- Success Modal -->
  <div id="successModal" class="hidden fixed inset-0 bg-black/40 flex items-center justify-center z-50 px-4">
    <div class="bg-white p-6 rounded-xl w-full max-w-sm text-center shadow-lg">
      <h3 class="text-2xl font-bold text-green-600">Payment Successful ✅</h3>
      <p id="successText" class="mt-3 text-sm text-zinc-700"></p>
      <div class="mt-4">
        <button onclick="closeSuccess()" class="bg-indigo-600 text-white px-4 py-2 rounded">Done</button>
      </div>
    </div>
  </div>

<script>
  const API_BASE = (location.hostname === 'localhost' || location.hostname === '127.0.0.1') 
    ? 'http://localhost:3000' : ''; // adjust if hosted

  function openEnrollForm(course, amount) {
    document.getElementById('courseName').value = course;
    document.getElementById('courseAmount').value = amount;
    document.getElementById('showCourse').innerText = course;
    document.getElementById('showAmount').innerText = amount;
    document.getElementById('enrollModal').classList.remove('hidden');
  }

  function closeEnrollForm() {
    document.getElementById('enrollModal').classList.add('hidden');
  }

  function closeSuccess() {
    document.getElementById('successModal').classList.add('hidden');
    // optionally reset form
    document.getElementById('enrollForm').reset();
  }

  document.getElementById('enrollForm').addEventListener('submit', async function(e){
    e.preventDefault();
    const name = document.getElementById('studentName').value.trim();
    const email = document.getElementById('studentEmail').value.trim();
    const phone = document.getElementById('studentPhone').value.trim();
    const course = document.getElementById('courseName').value;
    const amount = Number(document.getElementById('courseAmount').value);

    if(!name || !email || !phone) { alert('Please fill all fields'); return; }

    // 1) create an order on the server (Razorpay order)
    try {
      const res = await fetch(API_BASE + '/create-order', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({ amount: amount, currency: 'INR', receipt: 'rcpt_' + Date.now() })
      });
      const orderData = await res.json();
      if(!orderData || !orderData.id) throw new Error('Could not create order');

      // 2) open Razorpay checkout with order_id
      const options = {
        key: orderData.key, // from server (Razorpay Key ID)
        amount: orderData.amount, // in paise
        currency: orderData.currency,
        name: "Zero→Hero Academy",
        description: `${course} Enrollment`,
        order_id: orderData.id,
        prefill: { name, email, contact: phone },
        handler: async function (paymentResp) {
          // paymentResp contains razorpay_payment_id, razorpay_order_id, razorpay_signature
          // save to backend DB
          const saveRes = await fetch(API_BASE + '/save-enrollment', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({
              name, email, phone, course, amount,
              paymentId: paymentResp.razorpay_payment_id,
              orderId: paymentResp.razorpay_order_id,
              signature: paymentResp.razorpay_signature
            })
          });
          const saved = await saveRes.json();
          document.getElementById('successText').innerText = `Saved: ${saved.saved ? 'Yes' : 'No'}. Payment id: ${paymentResp.razorpay_payment_id}`;
          closeEnrollForm();
          document.getElementById('successModal').classList.remove('hidden');
        },
        modal: { ondismiss: function(){ console.log('Checkout closed') } }
      };

      const rzp = new Razorpay(options);
      rzp.open();
    } catch(err) {
      console.error(err);
      alert('Error creating payment. See console for details.');
    }
  });


  
</script>
</body>
</html>
